import com.sun.source.tree.Tree;
import java.util.*;
public class AmrCodeforces {
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        int k=sc.nextInt();
        TreeMap<Integer,Integer> map=new TreeMap<>();
        int arr[]=new int[n];
        for(int i=0;i<n;i++){
            arr[i]=sc.nextInt();
        }
        int sum=0;
        int point=-1;
        for(int i=0;i<n;i++){
            sum=sum+arr[i];
            if(sum)
        }
    }
}
